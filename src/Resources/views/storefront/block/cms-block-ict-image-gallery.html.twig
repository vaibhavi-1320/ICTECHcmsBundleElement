{% block block_ict_image_gallery %}
    {% set element = block.slots.getSlot('gallery') %}

    {% if element %}
        <div class="cms-block-ict-image-gallery">
            <div class="cms-block-container-row">
                <div class="cms-block-ict-image-gallery__col" data-cms-element-id="{{ element.id }}">
                    {% sw_include '@Storefront/storefront/element/cms-element-' ~ element.type ~ '.html.twig' ignore missing with { element: element } %}
                </div>
            </div>
        </div>
    {% endif %}
{% endblock %}
